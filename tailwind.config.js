/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html',              // Asegúrate de incluir el archivo HTML
    './src/**/*.{js,jsx,ts,tsx}' // Escanea todos los archivos JS, JSX, TS y TSX dentro de src
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
